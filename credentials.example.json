{
  "//": "Template for local EAS credentials (eas build --local with credentialsSource: local).",
  "//2": "CI writes the real credentials.json from repo secrets — see .github/workflows/android-playstore.yml.",
  "//3": "Never commit a real credentials.json or the keystore; both are gitignored.",
  "android": {
    "keystore": {
      "keystorePath": "credentials/upload-keystore.jks",
      "keystorePassword": "REPLACE_ME",
      "keyAlias": "REPLACE_ME",
      "keyPassword": "REPLACE_ME"
    }
  }
}
